import React from 'react';
import './Reviews.css';

let ReviewsTitle = function(props) {
  return (
    <React.Fragment>
      {props.title}
    </React.Fragment>
  );
};

export default ReviewsTitle;